apiVersion: v1
kind: Pod
metadata:
  name: gpu-test-pod
  labels:
    purpose: gpu-test
spec:
  containers:
  - name: gpu-container
    image: nvidia/cuda:11.0-base
    resources:
      limits:
        nvidia.com/gpu: 1
    command: ["nvidia-smi", "-L"]
  restartPolicy: Never